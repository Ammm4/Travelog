import React from 'react';
import { ForumDescription } from '../GlobalComponents/StyledComponents/Containers';

export default function Description({ body }) {
  return (
    <ForumDescription>{ body }</ForumDescription>
  )
}
